[project]
name = "tape-archiver"
version = "0.1.0"
description = "Simulated tape archiver CLI"
readme = "README.md"
requires-python = ">=3.8"
authors = [{name = "Axentx"}]
license = {text = "MIT"}

[tool.setuptools.packages.find]
where = ["src"]
